<div>
  <div>
    <h2> STOCK & TRADE</h2>
  </div>
 <% if user_signed_in? %>
  <% if current_user.is_admin? %>

    <div class="nav-top-buttons">
      <%= link_to 'Home', root_path, class: 'button' %>
      <%= link_to 'Transaction', admin_transactions_path, class: 'button' %>
      <%= link_to 'Pending Sign-ups', pending_admin_users_path, class: 'button' %>
    </div>

    <div class="nav-bottom-buttons">
      <%= link_to "Sign Out", destroy_user_session_path, data: { turbo_method: :delete }, class: 'button'%>
    </div>
    <div>
      <%= link_to "Edit Profile", edit_user_registration_path, class: 'button'%>
    </div> 
    
  <% else %>

    <div class="nav-top-buttons">
      <%= link_to 'Home', root_path, class: 'button' %>
      <%= link_to "My Portfolio", trader_my_portfolio_path, class: 'button' %>
      <%= link_to "Transaction", trader_transactions_path, class: 'button' %>
    </div>

    <div class="nav-bottom-buttons">
      <%= link_to "Sign Out", destroy_user_session_path, data: { turbo_method: :delete }, class: 'button'%>
    </div>
    <div>
      <%= link_to "Edit Profile", edit_user_registration_path, class: 'button'%>
    </div> 
  <% end %>

<% else %>

    <div>
      <%= link_to "Sign Up", new_user_registration_path%>
    </div>
    <div>
      <%= link_to "Sign In", new_user_session_path%>
    </div>

<% end %>
</div>